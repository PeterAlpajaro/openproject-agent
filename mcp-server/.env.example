# OpenProject instance base URL (internal Docker hostname or localhost).
OPENPROJECT_URL=http://openproject:80

# OpenProject API token. Used as the HTTP Basic Auth password with username "apikey".
OPENPROJECT_API_TOKEN=your-openproject-api-token-here

# Port the MCP server listens on.
PORT=3000
